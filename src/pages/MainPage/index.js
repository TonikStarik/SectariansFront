import React from 'react';

import { Text, ScrollView } from "react-native";

const MainPage = () => {
  return (
    <ScrollView>
      <Text>Main</Text>
    </ScrollView>
  );
}

export default MainPage;
