import React from 'react';

import { Text, ScrollView } from 'react-native';

const SignIn = () => {
  return (
    <ScrollView>
      <Text>SignIn</Text>
    </ScrollView>
  );
};

SignIn.propTypes = {};

export default SignIn;
